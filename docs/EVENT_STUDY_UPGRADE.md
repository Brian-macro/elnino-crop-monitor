# 历史复盘与Brian品牌更新

使用用户原始二维码与头像，不生成替代图片；站名统一“Brian的厄尔尼诺看盘”，关于页使用用户提供文案。首页完整移除“查看历史、价格与预测修正”及懒加载入口。

复盘数据：国内主连C0/B0（另存A0）/SR0/RR0/WH0；外盘C/S/RS/W通过AkShare同源新浪JSONP接口获取原始字节并归档。期货独立表，不与已有现货价格混合；非交易报价不计入走势；当天未完成日线排除；未知成交量为空。主连换月规则由供应商提供，不假造实际月份合约。

前端事件窗T±12月：内盘主连、外盘期货（没有匹配数据可显式选现货参考）、ONI；价格统一观测共同基期=100，不把原币种混在同轴。缺月不补，缺品种/年份显示“数据缺失”。

年度侧栏：选择事件涉及市场年度，显示Global/China产量、期末库存、国内消费与库存消费比；PSD与WASDE各自查询同一文档和年度，无法获取时留空，不偷偷替换。PSD的Global按其国家全集派生合计；WASDE直接World行。中国PSD在此作为用户授权的USDA可比复盘口径，不改变首页中国独立来源。

验证：JSONP安全解析、完成日线/未成交过滤、共同指数基期、缺失值与零分母；真实下载、原始哈希、幂等重跑、现货表未混期货；前端事件/品种/年份切换、侧栏与二维码头像展示、手机布局和构建。

糖的PSD消费字段为Total Disappearance（食用消费+其他国内消耗），旧归一化器未接此字段，本次已补齐并从原始ZIP重放，不重复加算Human Dom. Consumption。个别国家原始消费为负被既有质量规则隔离，全球分母不完整时库存消费比显示数据缺失，不将该国家默认为零。

## 交付验证

- 35,328条期货日线，10个连续系列；重放全部原始JSONP后行数不变，现货表仍为4,036条。每天到期更新已加入统一调度。
- 63项Python测试通过，数据库完整性与原始文件SHA256校验0失败，TypeScript/Next生产构建通过。
- 从便携Parquet恢复新库，35,328条期货记录完整恢复；临时库与本次格式化依赖均已清理。
- Playwright桌面/手机验证：新站名、原图圆形头像、用户介绍逐句、原二维码、移除首页入口、2023玉米内外盘与ONI、PSD/WASDE年度切换、1997缺失期货与显式现货参考、豆/糖/米/麦切换全部通过，无页面错误或横向溢出。
- 二维码JPEG与头像JPEG的SHA256与用户附件一致；头像WebP为同一图片的等比缩小版。
- 复盘默认采用价格指数，不冒充人民币价差或进口平价。USD/蒲式耳与CNY/吨不直接混轴。
- 可选动态接口：`/api/events/{crop}`；静态站读取`/api/events_<crop>.json`。本地网页3000、静态预览3001、API8010继续运行。
