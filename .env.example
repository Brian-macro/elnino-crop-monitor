# Empty for localhost and custom domains. GitHub project Pages: /repository-name
NEXT_PUBLIC_BASE_PATH=
# Defaults to the supplied public/images/brian-wechat.jpg; optional override below.
NEXT_PUBLIC_WECHAT_IMAGE=
# Optional absolute database path for Python (otherwise data/processed/monitor_v2.duckdb)
# MONITOR_DB=C:/path/to/monitor_v2.duckdb
# MONITOR_OFFLINE=1 reads previously archived originals; it is not a live freshness check.
